#!/usr/bin/env bash

ORIGIN=$HOME/.files/vim
TARGET=$HOME/.vim

mkdir -p "$(dirname "$TARGET")"
rm -rf "$TARGET"
ln -sf "$ORIGIN" "$TARGET"


ORIGIN=$HOME/.vim/init.vim
TARGET=$HOME/.vimrc
ln -sf "$ORIGIN" "$TARGET"
