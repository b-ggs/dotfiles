name: eotd
root: ~/dev/b-ggs/essaysonthedot
pre_window: rvm use 2.5.1@essaysonthedot
windows:
  - nvim:
    - nvim
  - server:
      layout: even-horizontal
      panes:
        - bundle exec rails s
        - bundle exec rails c
