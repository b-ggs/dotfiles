name: ind
root: ~/dev/jessicamah/indinero

windows:
  - editor:
      - vim
  - server: 
      layout: even-horizontal
      panes:
        - sleep 20 && be rails s
        - sleep 20 && be rails c
  - services: 
      panes:
        - cd ~/dev/jessicamah/indinero_vagrant_box && vagrant up && vagrant ssh
        - be rake sunspot:solr:start
