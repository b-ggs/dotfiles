name: ind
root: ~/dev/indinero

windows:
  - vim:
      - vim
  - server: 
      layout: main-vertical
      panes:
        - sleep 20 && be rails s
        - sleep 20 && be rails c
  - services: 
      root: ~/dev/indinero_vagrant_box
      panes:
        - vagrant up && vagrant ssh
