name: ind
root: ~/dev/indinero

windows:
  - vim:
      - vim
  - server: 
      layout: even-vertical
      panes:
        - sleep 20 && be rails s
        - sleep 20 && be rails c
  - services: 
      panes:
        - cd ~/dev/indinero_vagrant_box && vagrant up && vagrant ssh
        - be rake sunspot:solr:start
